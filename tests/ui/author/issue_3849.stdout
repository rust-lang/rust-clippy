if let StmtKind::Let(local) = stmt.kind
    && let Some(init) = local.init
    && let ExprKind::Call(func, args) = init.kind
    && cx.is_path_diag_item(func, sym::transmute)
    && args.len() == 1
    && let PatKind::Wild = local.pat.kind
{
    // report your lint here
}
