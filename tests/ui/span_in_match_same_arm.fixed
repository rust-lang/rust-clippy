#![warn(clippy::match_same_arms)]

fn main() {
    let x = 2;

    match x {
        2 => println!("different"),
        1 | 3 => println!("same"),//~ ERROR: these match arms have identical bodies
        _ => println!("other"),
    }
}