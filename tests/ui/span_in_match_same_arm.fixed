#![warn(clippy::match_same_arms)]

fn main() {
    let x = 2;

    match x {
        2 => println!("different"),
        //~ ERROR: these match arms have identical bodies
        1 | 3 | 4 => println!("same"),
        _ => println!("other"),
    }
}