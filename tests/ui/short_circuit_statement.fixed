#![warn(clippy::short_circuit_statement)]
#![allow(clippy::nonminimal_bool)]

fn main() {
    if f() { g(); } //~ short_circuit_statement
    if !f() { g(); } //~ short_circuit_statement
    if 1 != 2 { g(); } //~ short_circuit_statement
}

fn f() -> bool {
    true
}

fn g() -> bool {
    false
}
