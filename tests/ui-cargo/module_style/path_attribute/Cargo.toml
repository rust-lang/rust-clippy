# Should lint `mod` declarations tagged with `#[path = ...]`
[package]
name = "path-attribute"
version = "0.1.0"
edition = "2018"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
